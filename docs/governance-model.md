# Governance Model

Governance decisions are made before every tool invocation:

1. evaluate policy for actor + tool + risk
2. enforce allow/deny/require_approval outcome
3. enforce hard budget cap before execution
4. persist decision and state events for audit
